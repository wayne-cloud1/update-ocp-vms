---
plugin: kubevirt.core.kubevirt
connections:
  - kubeconfig: ~/.kube/kubeconfig
  # host: https://api.ocp.squeeb.me:6443
    validate_certs: false
    namespace: aap
    # network_name: default
    #use_service: true
  # label_selectors:
  #   - app=production